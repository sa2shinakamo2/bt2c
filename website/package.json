{
  "name": "bit2coin-website",
  "version": "1.0.0",
  "description": "Official website for bit2coin - A sustainable blockchain platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.prod.js",
    "build": "npm install --production",
    "test": "echo \"No tests specified - passing by default\" && exit 0"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
